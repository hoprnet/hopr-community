{
  "name": "subgraph-hopr-mainnet",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --studio $DEPLOY_KEY",
    "deploy:studio": "graph deploy --studio $SUBGRAPH_SLUG",
    "deploy:hosted-prod": "graph deploy --product hosted-service --ipfs https://api.thegraph.com/ipfs hoprnet/hopr-on-mainnet --access-token $ACCESS_TOKEN"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.1",
    "@graphprotocol/graph-ts": "0.29.1"
  }
}
