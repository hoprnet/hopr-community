{
  "name": "subgraph-hopr-mainnet",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ hoprnet/hopr-on-mainnet",
    "create-local": "graph create --node http://localhost:8020/ hoprnet/hopr-on-mainnet",
    "remove-local": "graph remove --node http://localhost:8020/ hoprnet/hopr-on-mainnet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hoprnet/hopr-on-mainnet",
    "deploy-xdai:prod": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs hoprnet/hopr-on-mainnet --access-token $ACCESS_TOKEN"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.1",
    "@graphprotocol/graph-ts": "0.29.0"
  }
}
